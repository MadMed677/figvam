use legion::*;

use crate::components::{GraphicsComponent, PositionComponent, SizeComponent};

// #[system(for_each)]
// pub fn render_system(pos: &PositionComponent, graphics: &GraphicsComponent) {}
